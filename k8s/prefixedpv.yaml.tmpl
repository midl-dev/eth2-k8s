apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: eth2-private-node
spec:
  volumeClaimTemplates:
  - metadata:
      name: eth2-private-node-pv-claim
    spec:
      storageClassName: ${kubernetes_name_prefix}-regional
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 10Gi
